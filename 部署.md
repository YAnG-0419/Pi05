# FR3 双臂 + Wuji Hand 2 真机部署草案

更新时间：2026-09-07。本文依据当前本地代码整理，尚未在新工控机或真机上验证。

**当前可配置环境、启动模型服务、做远程只读推理；完整真机客户端尚待实现。** 文中的待开发模块不是已经存在的命令。

## 1. 两台机器如何分工

默认保留当前机器做 GPU 推理，另一台工控机直接连接真机。Mac 用于 SSH 操作和查看日志。

```text
当前 GPU Linux：openpi + 训练权重 + WebSocket 服务（8000）
                  ↑ 三路 RGB、54 维状态、任务指令
                  ↓ 50 × 54 维绝对关节目标
新工控机：观测适配 + 异步动作执行（待开发）
          ├─ ROS 2 → ArmCommand → 安全网关 → FR3 双臂
          ├─ WujiHand2Backend → Hand 2 SDK → 左右手
          └─ ROS 2 ← 三路相机、FR3 状态
```

工控机只做采图、读状态和控制时，不需要安装完整模型环境或复制训练数据、权重。GPU 服务也可迁到其他 GPU 机器；届时复制本仓库、完整 checkpoint 和所需模型缓存，并重新建环境。

## 2. 应使用哪些代码

| 代码 | 当前路径 | 用途 |
|---|---|---|
| 模型与训练适配 | `/home/descfly/lpy/openpi` | `pi05_fr3_wuji`、模型服务、轻量客户端 |
| 采集时的真机控制栈 | `/home/descfly/llx/gello_upper_body_teleop` | FR3 控制、安全网关、Hand 2 SDK 后端、相机与数据契约 |
| 部署结构参考 | `/home/descfly/lpy/wuji-openpi/examples/wuji` | 参考观测组织和异步执行；不能直接套用话题和手驱动 |

应迁移当前修改后的代码版本；只克隆官方 openpi 不包含本项目的 FR3/Wuji 适配。用 Git 提交/补丁或文件同步保留本地改动，记录两个仓库的 commit 和工作区差异。

不要复制 `.venv`、Conda 环境目录或 `ros_ws/build`、`install`、`log` 到新机器继续使用。按源码重建。设备配置、未跟踪的相机配置和必要标定需单独核对迁移。

## 3. 当前 GPU 机器：openpi 环境

### 3.1 检查现有环境

```bash
cd /home/descfly/lpy/openpi
nvidia-smi
uv --version
uv run python -c 'import sys, jax; print(sys.version); print(jax.devices())'
```

确认 JAX 列表包含 GPU。当前 `.python-version` 为 3.11；依赖由 `pyproject.toml`、`uv.lock` 管理，包括 `jax[cuda12]==0.5.3`。驱动兼容性以安装与 GPU 检查结果为准。

现有环境可用时直接复用。需要重建时，先安装 Git、Git LFS、uv 和适用的 NVIDIA 驱动，然后在本仓库运行：

```bash
git submodule update --init --recursive
GIT_LFS_SKIP_SMUDGE=1 uv sync --locked
GIT_LFS_SKIP_SMUDGE=1 uv pip install -e .
uv run python -c 'import jax; print(jax.devices())'
```

不要将 ROS 的 Python 包装入这个模型环境。显存是否足够以本任务实际加载和推理测试为准。

### 3.2 权重和归一化文件

当前已存在的候选 checkpoint：

```text
checkpoints/pi05_fr3_wuji/tomato_lora_65ep/19999
checkpoints/pi05_fr3_wuji/tomato_lora_65ep/10000
```

迁移 checkpoint 时至少保留完整 `params/` 和 `assets/`，最稳妥是复制整个步骤目录。归一化文件应从该 checkpoint 的 `assets/fr3_wuji/tomato/norm_stats.json` 读取，不要拿其他实验的统计文件替换。

首次在新机器运行还可能下载 tokenizer 等模型资源；正式测试前完成一次加载和预热。

### 3.3 启动模型服务（现有命令）

```bash
cd /home/descfly/lpy/openpi
uv run scripts/serve_policy.py --port=8000 policy:checkpoint \
  --policy.config=pi05_fr3_wuji \
  --policy.dir=checkpoints/pi05_fr3_wuji/tomato_lora_65ep/19999 \
  --default-prompt="Pick up a tomato truss with the right hand, then pick a cherry tomato with the left hand and place it in the left basket."
```

当前脚本固定监听 `0.0.0.0`，没有 `--host` 参数。客户端若传 `prompt`，会覆盖默认指令；部署时使用上述训练任务原文。

服务留在独立终端或持久会话中。8000 端口只需在工控机与 GPU 机器之间可达，不需要把 ROS DDS 扩展到 GPU 机器。

## 4. 新工控机：真机环境

### 4.1 沿用采集仓库的环境结构

采集仓库的实际结构是：

- ROS 控制与相机：Docker，基础镜像 `ros:humble-ros-base`。
- 宿主机 Operator/Hand SDK：Conda，Python 3.10。
- 当前 GPU 主机上的 `/opt/ros/jazzy` 不代表采集栈使用 Jazzy。不要直接将 Humble 生成的工作区或消息包与 Jazzy 混用。

新机可按采集仓库说明采用 x86-64 Ubuntu 22.04。准备 Docker Engine、Compose 插件、Conda/Miniforge、Git、CMake、编译工具及 `lsof`。详细步骤见该仓库 `docs/NEW_COMPUTER_SETUP.md`。

以下假设工控机源码位于 `~/robot_ws/gello_upper_body_teleop`，openpi 位于 `~/robot_ws/openpi`；按实际路径替换。

### 4.2 生成本机配置并构建 ROS 工作区

```bash
cd ~/robot_ws/gello_upper_body_teleop
cp docker/.env.example docker/.env
```

编辑配置后再构建，重点核对：

| 文件/字段 | 新机器需要修改或核对 |
|---|---|
| `docker/.env`：`TELEOP_DATA_ROOT` | 新机器上的数据目录 |
| `FRANKA_CPUSET`、`FRANKA_TELEOP_CPUSET`、`HOUSEKEEPING_CPUSET` | 依据新工控机 CPU 拓扑分配，不能直接复制旧核号 |
| `config/workcell/current.yaml`：`controller_cpus` | 与 CPU 分配一致；原文件为左 `8-9`、右 `12-13` |
| 左右 `robot_ip`、网卡地址 | 当前配置为 `172.16.0.2`、`172.16.1.2`，以现场为准 |
| `TELEOP_ROS_DOMAIN_ID`、Cyclone DDS 配置 | 同一工控机的 ROS 参与者保持一致 |
| 左右 Hand 2 地址 | 显式填写各自 `IP:PORT`，启动时核对 handedness |

Franka FCI 网口保留给机械臂实时通信；GPU 推理通信走普通局域网。实时内核、CPU/IRQ 调优参考仓库现有文档和 `ops/setup/*franka*` 脚本，在新机核对后配置，不能照搬旧机器参数。

```bash
./ops/setup/build.sh
```

该命令构建镜像和 ROS 工作区，不是启动真机控制。当前 Dockerfile 默认 libfranka 0.19.0、franka_ros2 v2.0.4；迁移时仍需核对现场 FR3 系统版本的兼容性。

### 4.3 Hand 2 / 宿主机 Python 环境

若完整复现采集仓库的宿主机环境，可运行现有安装脚本：

```bash
cd ~/robot_ws/gello_upper_body_teleop
./ops/setup/setup_teleop_env.sh
./ops/setup/setup_wuji_env.sh
conda run -n gello-upper-body-teleop python -c \
  'import wuji_sdk; from adapters.wuji.backend import WujiHand2Backend; print("Hand 2 imports OK")'
```

这一步导入后端，不实例化、不连接手。记录成功安装的版本，用于后续复现：

```bash
conda run -n gello-upper-body-teleop python -m pip freeze > hand-environment-freeze.txt
```

上述脚本会安装遥操相关依赖；策略执行本身不需要 MANUS 重定向或 GELLO 输入。后续可以为独立 Hand 执行进程精简依赖，但应先验证 SDK 和现有后端一致。

不要直接把模型客户端全部装进含 Pinocchio/NLopt 的 Conda 环境：`openpi-client` 要求 NumPy `<2`，先检查依赖是否兼容。推荐 ROS/策略客户端与宿主机 Hand SDK 进程分开，通过明确的本地命令与反馈协议连接；该协议尚待开发。

### 4.4 ROS 策略客户端环境（待新增容器服务）

建议在 Humble 控制镜像基础上增加独立策略客户端镜像/Compose 服务：

- 挂载工控机上的 `openpi` 源码，例如 `/workspace/openpi`。
- 使用镜像里的 Python 3.10、`rclpy`、已编译的 `teleop_interfaces`。
- 安装 `cv_bridge`、OpenCV、轻量 `openpi-client`。
- 沿用控制容器的 host 网络、ROS domain、Cyclone DDS 和 housekeeping CPU 配置。

下面是新增镜像或容器环境内需要执行的安装内容，不是宿主机模型环境安装命令：

```bash
apt-get update
apt-get install -y ros-humble-cv-bridge python3-opencv
python3 -m pip install -e /workspace/openpi/packages/openpi-client 'typing_extensions>=4.12.2'
python3 -m pip check
python3 -c 'import rclpy, cv_bridge; from teleop_interfaces.msg import ArmCommand; from openpi_client import websocket_client_policy; print("ROS client imports OK")'
```

`typing_extensions` 是当前客户端代码实际使用的依赖，因此显式安装。将安装步骤固化到新 Dockerfile，避免只在临时容器中安装后丢失。现有 Compose 尚未定义这个策略客户端服务。

### 4.5 相机配置

复制并填写采集仓库的本地相机配置：

```bash
cd ~/robot_ws/gello_upper_body_teleop
cp data_collection/config/cameras.yaml.example data_collection/config/cameras.yaml
```

填写三台相机的序列号和实际语义，并与训练采集一致。当前契约使用 `/cam0/color/image_raw`、`/cam1/color/image_raw`、`/cam2/color/image_raw`；cam0 视频 20 Hz，cam1/cam2 为 30 Hz，数据行按 30 Hz 采样。

仓库对 bundled Orbbec runtime 有许可证构建开关：按其 `docker/.env.example` 和相机说明阅读对应协议后，在新机配置接受状态，并按要求使用 `./ops/setup/build.sh --accept-orbbec-eula`。普通构建不启用这套 bundled 相机运行时。该选项是仓库现有机制。

模型推理只用三路 RGB，不用深度。要单独补充相机启动入口，避免为了采图启动整套遥操和录制流程。

## 5. 先验证跨机器推理（不连接机器人）

工控机可以先在一个独立轻量环境测试网络，不依赖 ROS、SDK 或 GPU。源码已放在 `~/robot_ws/openpi` 后运行：

```bash
python3 -m venv ~/venvs/openpi-netcheck
source ~/venvs/openpi-netcheck/bin/activate
python -m pip install -e ~/robot_ws/openpi/packages/openpi-client 'typing_extensions>=4.12.2'
export POLICY_HOST=192.168.10.10  # 替换为 GPU 机器的局域网 IP
python - <<'PY'
import os
import time
import numpy as np
from openpi_client.websocket_client_policy import WebsocketClientPolicy

client = WebsocketClientPolicy(host=os.environ['POLICY_HOST'], port=8000)
obs = {
    'observation/state': np.zeros(54, dtype=np.float32),
    'observation/image': np.zeros((224, 224, 3), dtype=np.uint8),
    'observation/left_wrist_image': np.zeros((224, 224, 3), dtype=np.uint8),
    'observation/right_wrist_image': np.zeros((224, 224, 3), dtype=np.uint8),
    'prompt': 'Pick up a tomato truss with the right hand, then pick a cherry tomato with the left hand and place it in the left basket.',
}
for index in range(3):
    start = time.monotonic()
    actions = np.asarray(client.infer(obs)['actions'])
    print(index, actions.shape, 'finite=', np.isfinite(actions).all(),
          'seconds=', time.monotonic() - start)
    assert actions.shape == (50, 54) and np.isfinite(actions).all()
PY
```

验收：返回 `(50, 54)`，没有 NaN/Inf，记录首次预热和后续耗时。这些是假观测，输出只用于通信验证，绝不能发给机器人。测试完成后退出环境：`deactivate`。

## 6. 真机输入输出契约

### 6.1 观测

| 输入 | 来源与模型键 |
|---|---|
| 左右臂实测位置 | `/left/franka/joint_states`、`/right/franka/joint_states` |
| 左右手实测位置 | SDK `read_position()`；或独立 Hand 执行进程发布的新鲜反馈 |
| 三路 RGB | cam0 → `observation/image`；cam1 → `observation/left_wrist_image`；cam2 → `observation/right_wrist_image` |
| 状态 | `observation/state`：`[左臂7、左手20、右臂7、右手20]`，54 维 float32，弧度 |

双臂按关节名称重排，手部按采集契约的 20 关节固件顺序。不要用目标命令冒充实测状态。RGB 使用 HWC、uint8，核对颜色及采集时视角。预处理沿用 openpi 等比例补边逻辑，先验证与服务端输入一致。

如输入 108 维，必须严格遵守 `data_collection/contract/dataset_contract.yaml`，服务端才会正确切片。直接拼 54 维更简单。

### 6.2 输出

输出每步为绝对关节位置，弧度：左臂 `0:7`、左手 `7:27`、右臂 `27:34`、右手 `34:54`。服务端已反归一化并将臂部 delta 还原，不要再次累加当前关节状态。

FR3：发往 `/teleop/arm_commands`，类型 `teleop_interfaces/msg/ArmCommand`。填写 `source=openpi`、session、递增 sequence、active_sides、joint_names、positions。双臂命令应重新拼成左臂7 + 右臂7；命令名称是 `left_fr3v2_joint1..7`、`right_fr3v2_joint1..7`，不要用数据集的名称代替。先在网关配置中加入来源 `openpi`，保留既有接管、限位和超时检查。

Hand 2：复用 `adapters/wuji/backend.py` 的 `WujiHand2Backend.send()`。训练手部命令已是固件顺序，不再调用 MANUS 重定向或重复应用关节排列变换。

**`/teleop/wuji/{side}/command` 是采集遥测，不是执行入口。** `teleop_hand_telemetry/receiver.py` 只有发布器，向该话题发消息不会让这份代码驱动手。原 Operator 退出后，其手部状态遥测也会消失，部署执行进程必须负责持续读反馈。

## 7. 尚需开发的模块

建议模型相关代码留在 openpi，真机执行与安全接入放在采集控制仓库的独立部署入口。以下名称仅为建议：

| 模块 | 必须完成的行为 |
|---|---|
| `observation_adapter` | 收集三路 RGB、实测关节；按时间戳同步；拒绝缺失/过期数据 |
| `policy_client` | 远程推理、预热、超时、错误处理和推理耗时统计 |
| `action_executor` | 与推理解耦，30 Hz 消费动作；首步和 chunk 衔接检查；异常时协调双臂双手 |
| `fr3_policy_source` | ArmCommand 会话、序号、名称；接入安全网关；读取命令接受/拒绝状态 |
| `hand_executor` | 唯一 SDK 所有者；命令/反馈本地协议；位置/速度约束、诊断、启停和命令过期处理 |
| 部署配置及入口 | IP、相机、关节映射、限值、运行时长；默认不执行动作；开始/暂停/停止 |
| 运行记录 | 观测时间、推理动作、实际下发值、反馈、队列状态、网关拒绝原因 |

模型 horizon 保持 50；30 Hz 下覆盖约 1.67 秒。可配置每次实际执行多少步再重规划，但须依据测得延迟设计，不要随意改模型配置。网关的 0.25 秒命令超时要求控制循环独立于阻塞推理。队列耗尽必须进入明确的保持/停止状态，不能无限重放旧 chunk 或伪造新鲜观测。

Wuji 参考仓库有 RTG broker，而当前 openpi 客户端没有；首版可用异步队列，之后再评估是否移植 RTG。现有 WebSocket 客户端接收会阻塞，需补充执行端独立的超时监控。

## 8. 联调和正式启动顺序

1. 两端依赖导入通过，假观测远程推理通过。
2. 在工控机核对 FCI 网络、实时配置、左右设备身份和相机对应关系。
3. 启动 GPU 服务并预热。
4. 使用待新增的部署入口启动 FR3、安全网关、相机、Hand 执行进程；保持策略输出关闭。
5. 运行真机只读推理，检查数据新鲜度、输出范围、首步跳变与端到端耗时。
6. 分别验证少量臂/手动作和停止行为，再做有限步数的双臂双手闭环。
7. 场景与初始姿态接近采集条件后，测试完整任务，对比 10000 和 19999 权重。

不要把 `start_wuji_teleop.sh` 或不带服务名的 `docker compose up` 当成策略部署启动命令：它们会拉起遥操或其他硬件服务。当前还没有一条已经完成并验证的真机策略启动命令。

停止遥操来源后再让策略接管；同一只手只能由一个执行进程持有。默认禁用硬件输出属于采集仓库现有约定；Hand 后端的构造参数 `auto_enable=True` 则需由新入口明确覆盖。注意 `auto_enable=False` 仍会连接设备并调用 disable，不属于纯只读网络测试。

## 9. 验收清单

- [ ] 固定两个仓库版本、依赖版本、checkpoint 和任务指令。
- [ ] GPU JAX 正常；远程推理输出 `(50,54)`；预热后延迟已记录。
- [ ] 新机 CPU、FCI 网络、ROS/DDS、设备身份已核对。
- [ ] 三相机与训练对应，实测状态顺序和弧度单位一致。
- [ ] FR3 的 openpi 来源接入网关，控制权交接和拒绝日志可见。
- [ ] Hand SDK 执行入口完成，不误用遥测话题。
- [ ] 初始跳变、限位、速度约束、断流、断网、队列耗尽和停止行为验证通过。
- [ ] 真机只读推理、小幅动作、短时闭环逐级完成。
- [ ] 记录预测、下发与反馈，区分模型效果和执行接口问题。

## 10. 本地依据

- [FR3/Wuji 模型适配](src/openpi/policies/fr3_wuji_policy.py)
- [训练配置](src/openpi/training/config.py)
- [模型服务](scripts/serve_policy.py)
- [远程推理说明](docs/remote_inference.md)
- [离线检查脚本](examples/fr3_wuji/replay_check.py)
- [微调纪要](docs/0906微调.md)
- 采集仓库：`docs/NEW_COMPUTER_SETUP.md`、`docker/Dockerfile`、`docker/compose.yaml`、`docker/.env.example`。
- 采集契约：`data_collection/contract/dataset_contract.yaml`。
- 控制接口：`ros_ws/src/teleop_core/teleop_core/contract.py`。
- Hand 2：`adapters/wuji/backend.py`、`adapters/wuji/pipeline.py`、`ros_ws/src/teleop_hand_telemetry/teleop_hand_telemetry/receiver.py`。
